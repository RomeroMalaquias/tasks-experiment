#include "channels_functions.h"

static int channel_open(CHANNEL *channel, const char *type_c, int window,int maxpacket, BUFFER *payload) {
   return 1;
}

int channel_open_session1(CHANNEL *channel){
return 2;
}
